function callAPItoAddLabel(text: string): string {
  //Call openai API here
  const fakeResultFromOpenAI = `お疲れ様です。
  コアさんの決定を応援します。問題ないです。:)`;
  return fakeResultFromOpenAI;
}

export default callAPItoAddLabel;
